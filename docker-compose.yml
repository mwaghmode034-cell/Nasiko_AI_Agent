services:
  hr-agent:
    build: .
    container_name: hr-agent
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    stdin_open: true
    ports:
      - "5000:5000"
    tty: true
    networks:
      - agents-net

networks:
  agents-net:
    external: true
